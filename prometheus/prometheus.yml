global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'load-test-db'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['load-test-db:8080']
        labels:
          application: 'load-test-db'

